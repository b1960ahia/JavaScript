{
  "name": "lotoanalise-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "Loterias — Analisador Avançado (Capacitor)",
  "scripts": {
    "sync": "npx cap sync",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
